NODE_ENV=development
PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_USER=tug
DB_PASSWORD=
DB_NAME=tug

# Comma-separated list of allowed CORS origins (admin portal, etc).
# Leave empty to allow all origins (convenient for local dev / mobile).
CORS_ORIGINS=http://localhost:3000
